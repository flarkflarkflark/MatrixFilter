name: Build MatrixFilter

on:
  push:
    branches: [ main, master, 'claude/**' ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential libgl1-mesa-dev libglu1-mesa-dev libx11-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libasound2-dev libjack-jackd2-dev libfreetype6-dev libxcb-util-dev libxcb-cursor-dev libxcb-keysyms1-dev libxcb-xkb-dev libxkbcommon-dev libxkbcommon-x11-dev libcairo2-dev libfontconfig1-dev libpango1.0-dev libgtk-3-dev pkg-config

      - name: Build VST3 plugin
        run: |
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release ..
          make -j$(nproc)

      - name: Upload VST3 artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MatrixFilter-Linux-VST3
          path: build/**/*.vst3
          if-no-files-found: error

  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Build VST3 plugin
        run: |
          mkdir build
          cd build
          cmake -G "Visual Studio 17 2022" -A x64 ..
          cmake --build . --config Release

      - name: Upload VST3 artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MatrixFilter-Windows-VST3
          path: build/**/*.vst3
          if-no-files-found: error

  build-macos:
    runs-on: macos-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          brew install cmake pkg-config

      - name: Build VST3 plugin
        run: |
          mkdir -p build
          cd build
          cmake -DCMAKE_BUILD_TYPE=Release -DCMAKE_OSX_DEPLOYMENT_TARGET=10.15 ..
          make -j$(sysctl -n hw.ncpu)

      - name: Upload VST3 artifacts
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: MatrixFilter-macOS-VST3
          path: build/**/*.vst3
          if-no-files-found: error
